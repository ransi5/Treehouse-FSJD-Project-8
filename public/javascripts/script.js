
// renders the last page when user selects perpage option in which the page requested
// does not exist or is less than the page requested than the total pages in new request.
  if (document.getElementsByTagName("td").length == 0) {
  document.filter.submit();
}
